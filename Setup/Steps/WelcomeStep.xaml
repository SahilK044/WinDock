<UserControl x:Class="WinDockSetup.Steps.WelcomeStep"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="680"
             Loaded="UserControl_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- App Badge -->
        <Border Grid.Row="0" HorizontalAlignment="Center" Margin="0,4,0,14" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="12" Padding="14,5">
            <TextBlock Text="WINDOCK v2.0  ·  OFFICIAL EDITION" FontFamily="{StaticResource MonoFontFamily}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TelemetryCyanBrush}"/>
        </Border>

        <!-- Title -->
        <TextBlock x:Name="TitleText" Grid.Row="1" Text="Welcome to WinDock" 
                   FontFamily="{StaticResource DisplayFontFamily}" FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource FogWhiteBrush}" 
                   HorizontalAlignment="Center" Margin="0,0,0,8" Opacity="0"/>

        <!-- Subtitle -->
        <TextBlock x:Name="SubtitleText" Grid.Row="2" Text="A high-performance macOS Dock experience for Windows 10 and 11." 
                   FontFamily="{StaticResource BodyFontFamily}" FontSize="14" 
                   Foreground="{StaticResource FogWhite70Brush}" 
                   HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Opacity="0"/>

        <!-- WinDock App Card -->
        <Grid x:Name="SpecPanel" Grid.Row="3" Margin="0,20,0,20" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0">
            <Border Width="380" Background="#FFFFFF" BorderBrush="#2563EB" BorderThickness="1.5" CornerRadius="14" Padding="24,20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="3" Opacity="0.08" Color="#2563EB"/>
                </Border.Effect>
                <StackPanel HorizontalAlignment="Center">
                    <Image Source="pack://application:,,,/windock_logo.png" Width="48" Height="48" HorizontalAlignment="Center" Margin="0,0,0,10" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="WinDock" FontFamily="{StaticResource DisplayFontFamily}" FontSize="18" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                    <TextBlock Text="macOS Dock · F1 Race Center · AirDrop File Sharing" FontFamily="{StaticResource BodyFontFamily}" FontSize="12" Foreground="#64748B" HorizontalAlignment="Center" TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Keep settings checkbox (uninstall mode only) -->
        <CheckBox x:Name="KeepConfigCheckBox" Grid.Row="4"
                  Content="Keep my config.json settings"
                  IsChecked="True"
                  Style="{StaticResource CyanCheckBoxStyle}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Visibility="Collapsed"/>

        <!-- Buttons Area -->
        <Grid x:Name="ButtonsPanel" Grid.Row="5" Margin="0,8,0,0" Opacity="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelBtn" Grid.Column="0" Style="{StaticResource GhostButtonStyle}" Content="Cancel" Click="CancelBtn_Click"/>

            <Button x:Name="ContinueBtn" Grid.Column="2" Style="{StaticResource PrimaryButtonStyle}" Content="Continue →" Click="ContinueBtn_Click"/>
        </Grid>
    </Grid>
</UserControl>